* {
    text:                       #4C4F69;
    foreground:                 #4C4F69;
    subtext:                    #6C6F85;

    pill:                       white / 82%;
    pill-soft:                  white / 68%;

    lavender:                   #7287FD;
    lavender-soft:              #E7E9FF;

    hover-bg:                   @lavender-soft;
    select-bg:                  @lavender;

    text-selected:              #FFFFFF;
    placeholder-color:          #6C6F85;

    background-alt:             @pill;
    selected:                   @select-bg;

    active:                     @hover-bg;
    urgent:                     @hover-bg;

    selected-normal-background: @select-bg;
    selected-active-background: @select-bg;
    selected-urgent-background: @select-bg;

    padding:                    0;
    spacing:                    0;
    border:                     0;
}

window {
    fullscreen:                 false;
    transparency:               "real";
    cursor:                     "default";
    background-color:           @pill;

    border:                     0;
    border-radius:              1.2em;

    /* IMPORTANT: restore original feel */
    padding:                    0;
}

mainbox {
    background-color:           transparent;

    /* FIX: prevents “split line” illusion */
    spacing:                    0;
}

inputbar {
    text-color:                 @foreground;

    /* keep original geometry */
    padding:                    12px 12px 4px 12px;

    children:                   [ prompt, textbox-prompt-colon, entry, num-filtered-rows, textbox-num-sep, num-rows ];

    background-color:           transparent;
}

listview {
    border:                     0;

    /* RESTORED original (important) */
    padding:                    2px 0 8px 0;

    spacing:                    0;

    fixed-height:               true;
    lines:                      12;
    scrollbar:                  false;

    background-color:           transparent;
}

element {
    padding:                    3px 12px;
    spacing:                    6px;
}

/* keep visuals unchanged */
element normal.normal,
element alternate.normal {
    background-color:           transparent;
    text-color:                 @text;
}

element normal.active,
element alternate.active {
    background-color:           @hover-bg;
    text-color:                 @lavender;
}

/* FIX ONLY: radius consistency */
element selected.normal {
    background-color:           @select-bg;
    text-color:                 @text-selected;
    border-radius:              1.2em;
}

element-text {
    background-color:           transparent;
    text-color:                 inherit;
    padding:                    0;
}

element-icon {
    background-color:           transparent;
    size:                       1em;
    margin:                     0 4px 0 0;
}

scrollbar {
    handle-color:               @lavender;
    width:                      4px;
    handle-width:               8px;
}

message {
    background-color:           @pill-soft;
    padding:                    2px 12px;
}

error-message {
    background-color:           transparent;
    padding:                    2px 12px;
}

textbox {
    background-color:           transparent;
    text-color:                 @text;
}

prompt {
    text-color:                 inherit;
    background-color:           transparent;
}

textbox-prompt-colon {
    text-color:                 inherit;
    margin:                     0 6px 0 0;
    expand:                     false;
    str:                        ":";
    background-color:           transparent;
}

entry {
    text-color:                 inherit;
    placeholder-color:          @placeholder-color;
    placeholder:                "Type to filter";
    background-color:           transparent;
}

num-filtered-rows,
num-rows,
textbox-num-sep {
    text-color:                 inherit;
    expand:                     false;
    background-color:           transparent;
}

textbox-num-sep {
    str:                        "/";
}
